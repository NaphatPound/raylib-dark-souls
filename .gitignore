# build output + fetched raylib source (regenerated by build.bat / CMake)
/build/
/build_out.txt
*.log

# transient screenshots written to the project root by the dev CLI modes
# (F2 in-game, plus the diag / scenic / auto demo runs)
shot.png
*.png.shot
/scenic*.png
/demo_*.png
/cmb_*.png
/_*.png

# runtime save file
savegame.txt
